import {Chef, FindUs, Header} from '../container';
import '../App.css';

const MainPage = () => (
    <div>
        <Header/>
        <Chef/>
        <FindUs/>
    </div>
);

export default MainPage;